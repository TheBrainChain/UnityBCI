using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Diagnostics;
using System;
using System.Threading;
using System.Net;
using System.Net.Sockets;
using System.Text;

public class Ball : MonoBehaviour {
		void Start () {
		//rb.AddForce ((new Vector3(UnityEngine.Random.Range (-15.0f,20.0f), 0f, UnityEngine.Random.Range (-15.0f,20.0f)).normalized) * speed, ForceMode.Impulse); // Create random impulse intially
		//rb.AddForce(initialImpulse, ForceMode.Impulse); //To Create an initial random hit
	}
		// Update is called once per frame
	void Update () {
		
	}
}
